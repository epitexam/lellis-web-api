
/**
 * DTO representing the input filters for searching networks.
 */
export interface ISearchNetworkInputDTO {
    /**
     * The display name of the network (e.g. "Hospital A", "Company B").
     * @example "Hospital A"
     */
    name: string;
}